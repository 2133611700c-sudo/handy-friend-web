name: Ops Health Smoke

on:
  workflow_dispatch:

jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Run production health smoke
        run: node scripts/ops-health-smoke.mjs
