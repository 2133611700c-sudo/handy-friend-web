---
id: INC-20260403-01
severity: SEV2
status: monitoring
started_at: 2026-04-03T03:00:00Z
resolved_at:
owner: operations
services: [openclaw, nextdoor, craigslist, intake]
impact: "Lead intake degraded: Nextdoor source checks dead, historical hunter runs skipped due stale lock, CL signal uncertain"
root_cause: "Stale lock directories + missing nextdoor_sources.json on this node + drift between expected ops_incidents schema and writer payload"
actions_taken:
  - "Patched run-hunter-agent.sh with stale lock auto-cleanup"
  - "Re-ran nextdoor hunter to confirm stale-lock recovery"
  - "Upgraded OpenClaw from 2026.3.24 to 2026.4.2"
  - "Connected OpenClaw browser dashboard (authorized, v2026.4.2 visible)"
  - "Executed openclaw_health_monitor.py and persisted local monitor artifacts"
prevention:
  - "Keep openclaw_health_monitor.py on 6h cron"
  - "Set explicit NEXTDOOR_SOURCES_PATH after real source file is restored"
  - "Align ops_incidents schema mapping for monitor writer"
---

# Incident Summary

## 1. Discovery Results
- OpenClaw binary: `/opt/homebrew/bin/openclaw`
- OpenClaw version before fix: `2026.3.24`
- OpenClaw version after update: `2026.4.2`
- OpenClaw root/config discovered:
  - `~/.openclaw/openclaw.json`
  - `~/.openclaw/agents/main/agent`
- Agents available: `main`, `claude-bridge` (no dedicated `nextdoor` agent configured)
- Channels configured in OpenClaw dashboard: none

## 2. Current State
- Historical hunter loop showed repeated `SKIP ... lock exists` for Nextdoor/Facebook.
- Lock dirs existed since Mar 31:
  - `/tmp/handyfriend-nextdoor.lock`
  - `/tmp/handyfriend-facebook.lock`
- `nextdoor_sources.json` not found on this node.
- Health monitor status currently RED (ND dead, CL stale by freshness window).

## 3. Findings
- Primary blocking failure was stale lock persistence, not only source emptiness.
- Legacy errors also present in historical logs:
  - `env: node: No such file or directory`
  - `Unknown agent id "nextdoor"`
- Current runner already falls back to `main` agent when `nextdoor` agent is absent.
- `ops_incidents` write path from health monitor failed due column mismatch (schema drift).

## 4. Root Cause
- **RC1:** lock mechanism had no stale-lock recovery path.
- **RC2:** source config artifact `nextdoor_sources.json` absent in this runtime.
- **RC3:** monitor incident payload did not match current `ops_incidents` schema.

## 5. Fix Applied
- Patched `scripts/run-hunter-agent.sh`:
  - added `LOCK_STALE_MINUTES` logic (default 180)
  - stale lock auto-removal before acquiring new lock
- Upgraded OpenClaw to latest available:
  - `2026.3.24 -> 2026.4.2`
- Authorized OpenClaw browser dashboard and verified online status.
- Ran health monitor (non-test) and generated artifacts:
  - `~/handy-friend-ops/logs/health_monitor.log`
  - `~/handy-friend-ops/logs/openclaw_health_monitor_latest.json`

## 6. Verification
- Evidence in `ops/hunter.log`:
  - `WARN nextdoor: stale lock ... removing`
  - `START nextdoor scan (agent=main)`
- OpenClaw dashboard (browser): authorized, version shown `v2026.4.2`, gateway online.
- Health monitor executed with exit 0 and produced digest/local JSON.

## 7. Remaining Risks
- **NOT VERIFIED:** real live Nextdoor feed fetch via `nextdoor_sources.json` (file missing).
- **NOT VERIFIED:** Craigslist 100% reject case for Dell Python hunter path (script path not present on this node).
- `ops_incidents` insert is currently best-effort and may skip due schema mismatch.

## 8. Next Immediate Action
1. Restore or define real `nextdoor_sources.json` path and set `NEXTDOOR_SOURCES_PATH`.
2. Run Prompt 4 monitor on Dell node with real source file present.
3. Map `ops_incidents` schema exactly and patch monitor writer once.
