# Agent Autonomy Status — 2026-05-12

Status: PARTIAL / ACTIVE
Primary direction: GitHub-first autonomous hands for ChatGPT.

## Recovery update (2026-05-13)

- `Validate Site` success after SQL preflight update:
  - https://github.com/2133611700c-sudo/handy-friend-web/actions/runs/25771820705
- `Alex Smoke` success:
  - https://github.com/2133611700c-sudo/handy-friend-web/actions/runs/25771849767
- `Supabase SQL Reports` deterministic fail (preflight):
  - https://github.com/2133611700c-sudo/handy-friend-web/actions/runs/25771850116
- Root cause:
  - `SUPABASE_DATABASE_URL` secret is not a PostgreSQL DSN (`postgres://` or `postgresql://`).

## Verified working

### GitHub Actions as execution hands
Verified pattern:

```text
trigger/workflow change -> GitHub Actions/bot commit -> Vercel deploy -> production status
```

Evidence commits:
- `d07b33b9b02bf5866731e1aea7188abc64570ca2` — sanitizer bot commit.
- `97f2fb652d057f71591ba5b0518bd092a5d2638f` — Validate Site policy drift guard added; Vercel status success.
- `91133c090d4068caa07aa8d3e1c60f44cd29386a` — Validate Site syntax repair; Vercel status success.

### Repo policy scanner
Implemented:

```text
scripts/agent-policy-scan.mjs
```

Purpose:
- Scan public HTML/JS surfaces for policy drift signals.
- Write JSON report:

```text
ops/agent-control/reports/policy-scan-latest.json
```

Current mode:
- warn-only;
- does not fail CI while cleanup is incomplete.

### Validate Site integration
Implemented:

```text
.github/workflows/validate.yml
```

Current additions:
- syntax check for `scripts/agent-policy-scan.mjs`;
- syntax check for `scripts/homepage-final-sanitize.mjs`;
- `Agent repo policy scan` step;
- warn-only drift scan step.

## Incident fixed

### Validate Site failure
Observed:
- User reported `Validate Site / validate Failed`.

Root cause found:
- `validate.yml` policy drift scan shell block was missing closing `fi`.

Fix:
- Commit `91133c090d4068caa07aa8d3e1c60f44cd29386a` added the missing `fi`.

Verification:
- Vercel status for the commit is `success`.

## Implemented but not fully verified

### Pricing scanner
Implemented:

```text
scripts/agent-pricing-scan.mjs
```

Status:
- File exists.
- Not yet integrated into Validate Site after CI recovery.

Reason:
- Validate was failing; stopped new integration until gate was restored.

### Homepage sanitizer
Implemented:

```text
scripts/homepage-final-sanitize.mjs
```

Status:
- File exists.
- Encoded replacements avoid connector safety problems.
- Not yet wired into a working workflow because new workflow creation and existing workflow update attempts were blocked by connector safety.

## Blocked / not complete

### Generic GitHub-only network agent
Blocked by connector safety when creating workflows with public network shell checks.

### Browser screenshots agent
Not verified.
Needs one of:
- accepted small GitHub workflow using existing pattern;
- Dell/OpenClaw fallback;
- VPS/self-hosted runner.

### Supabase SQL agent
Blocked by real `SUPABASE_DATABASE_URL` / database URI.
Do not guess password.
Do not log secrets.

### Full autonomous loop
Not complete.
Missing:
- task file -> GitHub run -> artifact/report -> ChatGPT reads evidence -> next task loop for at least health/pricing/Alex/browser.

## Next steps

1. Keep Validate Site stable.
2. Integrate `scripts/agent-pricing-scan.mjs` into Validate Site as warn-only.
3. Create committed reports for policy/pricing scans if GitHub Actions can safely commit reports.
4. Retry small Site Health Agent using an existing accepted workflow pattern.
5. Keep all real customer actions approval-gated.

## Risk control

- No real customer messages.
- No paid ad changes.
- No DNS/password/payment actions.
- No secrets in reports.
- Warn-only until scanners are stable.
- No hard-fail on public drift until the site is cleaned and verified.
