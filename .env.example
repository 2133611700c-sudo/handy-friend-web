# OpenAI
OPENAI_API_KEY=                 # OpenAI API access for scripts/automation

# Supabase
SUPABASE_URL=                   # Supabase project URL
SUPABASE_ANON_KEY=              # Public anon key (frontend/read-safe)
SUPABASE_SERVICE_ROLE_KEY=      # Server-only key for admin/RPC operations

# Deploy / CI
VERCEL_TOKEN=                   # Vercel deployment token
GITHUB_TOKEN=                   # GitHub API/CI automation token

# Billing / Payments
STRIPE_SECRET_KEY=              # Stripe server-side secret

# App
NEXT_PUBLIC_SITE_URL=           # Public site base URL (e.g., https://handyandfriend.com)
